<h1>Book <%= @animal.name %></h1>

<div>
  <%= simple_form_for [@animal, @booking] do |f| %>
  <%= f.input :start_date %>
  <%= f.input :end_date %>
  <%= f.submit "Book", class: "btn btn-flat" %>
  <% end %>
</div>
